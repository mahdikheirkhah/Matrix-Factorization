name: Recommender CI

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ main ]

# Fix for the Node.js 20 deprecation warning
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  quality-assurance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true
          plugins: poetry-plugin-export

      - name: Install dependencies
        run: poetry install --no-interaction --no-root

      - name: Check Code Formatting (Black)
        run: poetry run black . --check

      - name: Verify Audit Requirements (Imports)
        run: |
          poetry run python -c "import numpy; import pandas; import scipy; import streamlit; import matplotlib; print('✅ Audit Imports Passed')"
      
      - name: Security Check
        run: |
          pip install safety
          poetry export -f requirements.txt --output temp_req.txt --without-hashes
          safety check -r temp_req.txt

      - name: Run Tests with Coverage
        run: |
          # CRITICAL: Added --cov=utils so pytest-cov actually runs
          poetry run python -m pytest --cov=utils --cov-report=xml tests/

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          # Ensure this matches your exact GitHub URL casing
          slug: mahdikheirkhah/Matrix-Factorization
          verbose: true